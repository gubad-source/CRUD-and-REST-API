<div class="container">
    <div class="row">
        <div class="col-8 offset-2">
            <h1>Delete</h1>
            <div class="mb-3">
                <label for="authorId" class="form-label">
                    Author
                </label>
                <input id="authorId" name="Author" class="form-control" disabled value="@Model?.Author" />
            </div>
            <div class="mb-3">
                <label for="genreId" class="form-label">
                    Genre
                </label>
                <input id="genreId" name="Genre" class="form-control" disabled value="@Model?.Genre" />
            </div>
            <div class="mb-3">
                <label for="priceId" class="form-label">
                    Price
                </label>
                <input id="priceId" name="Price" class="form-control" disabled value="@Model?.Price" />
            </div>
            <form method="post">
                <fieldset>

                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-primary">
                        Sil
                    </button>
                </fieldset>
            </form>
        </div>
    </div>
</div>
